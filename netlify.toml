[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/ndbc/*"
  to = "https://www.ndbc.noaa.gov/data/latest_obs/:splat"
  status = 200
  force = true

# SPA fallback — all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
